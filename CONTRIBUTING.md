# 参与贡献

本仓库当前定位为笃学书院空间预约系统的校内试点项目。正式交付主线是 `miniprogram/` 原生微信小程序与 `backend/` FastAPI；`frontend/`、`cloudfunctions/` 仅供历史参考。

## 开始之前

1. 先搜索现有 Issue；安全漏洞请按 [安全政策](SECURITY.md) 私下报告。
2. 新功能、API 契约、数据库结构或权限规则变更应先建 Issue，并写清验收标准。
3. 禁止提交真实姓名、学号、手机号、OpenID、玉兰卡/清扫照片、访问令牌、AppSecret、数据库备份或 `.env`。
4. 校内身份验证方案、正式管理员凭据、许可证和隐私政策属于学校责任人决策，不应由贡献者自行假设。

## 开发流程

1. 从最新 `main` 创建短生命周期分支。
2. 修改正式主线代码，并同步测试、文档和已知限制。
3. 提交前运行：

   ```bash
   npm test
   npm run build:dev
   cd backend
   ../.venv/bin/python -m pytest -q
   ```

4. 创建 PR，关联 Issue，填写验证证据、隐私影响、迁移和回滚方案。
5. 等待至少一名维护者审核，并解决全部评审意见；不得绕过必需检查直接合并 `main`。

仓库默认 CODEOWNER 为仓库所有者；正式发布、安全、工作流、数据库和生产部署变更必须获得其审核。具体业务或学校责任人仍需按发布清单另行签字，CODEOWNER 批准不能替代学校验收。

## 变更边界

- 预约冲突、状态推进、限制和权限规则必须由后端执行，小程序只负责展示与交互。
- 数据库结构通过 Alembic 迁移，生产启动不得隐式改表。
- 敏感媒体必须通过私有媒体编号和管理员鉴权访问，不得恢复匿名静态 URL。
- 正式小程序只从 `miniprogram/` 构建，不得重新接入第一版云函数作为第二数据源。
- UI 延续暖橙 `#F25B15` 与紫色 `#6B46C1` 的正式视觉体系，并保留清晰的加载、空、失败与重试状态。

## Issue 分诊约定

维护者应在新 Issue 创建后的 2 个工作日内补充严重度、负责人或负责人角色、状态和目标里程碑。无法立即处理时也应标记阻塞条件，避免 Issue 长期处于无归属状态。

优先级含义：

- `P0`：安全、隐私、数据完整性或发布阻断；修复/风险接受前不得发布。
- `P1`：试点候选版必须完成的可靠性和主流程问题。
- `P2`：不阻断试点但影响维护、体验或推广的问题。
- `external-blocker`：依赖学校、微信平台、域名、凭据或书面政策。

## 提交与 PR

提交信息建议使用 `feat:`、`fix:`、`security:`、`test:`、`docs:`、`ops:`、`chore:` 前缀。一个 PR 聚焦一个可验收目标；大范围格式化、生成文件和业务修改不要混在同一提交中。

详细支持范围见 [SUPPORT.md](SUPPORT.md)，候选版门槛见 [发布基线](docs/PILOT_RELEASE_BASELINE.md)。
