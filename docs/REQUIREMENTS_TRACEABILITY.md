# 老师需求对照与 v2 验收说明

本文以当前正式架构为准：`miniprogram/` 是原生微信小程序 v2 主线，`backend/` 是唯一业务后端。`frontend/` 和 `cloudfunctions/` 仅作参考，不能用它们的页面或接口证明正式 v2 已完成。

状态含义：

- **已接入**：原生页面与 FastAPI 已形成调用链，可做本地业务验收。
- **后端具备**：FastAPI 已有能力，但原生页面尚无完整入口。
- **待平台配置**：代码链路存在，必须取得微信/学校正式资源后才能真机验收。
- **未接入**：当前只有限制说明或占位，不应宣称功能完成。

## 学生端与空间导览

| 需求 | 状态 | 正式 v2 实现与验收点 |
|---|---|---|
| 保留第一版紫色体验 | 已接入 | `miniprogram/app.wxss`、`custom-tab-bar/` 及各原生页面延续紫色主题；底部为“空间导览 / 我的” |
| 微信项目入口 | 已接入 | 微信开发者工具导入仓库根目录，由 `project.config.json` 加载 `miniprogram/` |
| 学号密码登录 | 已接入 | `pages/login/login` → `POST /api/auth/login`，JWT 存入微信本地存储 |
| 学生注册 | 已接入 | `pages/login/register` 采集学号、姓名、手机号、4 位班级和密码；前后端均校验 |
| 班级显示 | 已接入 | 数据库存 4 位数字；`pages/my/my` 以“笃学2601”形式展示 |
| 忘记密码 | 未接入 | `pages/login/forgot` 仅提示联系管理员；没有短信验证码发送/重置 API |
| 微信免密自动登录 | 待平台配置 | 无本地 token 时调用 `wx.login` → `POST /api/auth/wechat/login`；首次密码登录/注册后后台绑定 OpenID，需正式 AppID/AppSecret 才能验收 |
| 空间导览与楼层图 | 已接入 | `pages/index/index` 调用 `GET /api/rooms`，显示可预约、公开、辅导员和不可互动空间 |
| 公开空间状态 | 已接入 | 首页和 `pages/room/room` 展示 free/busy/crowded；管理员可在房间页修改 |
| 房间今日占用 | 已接入 | `pages/room/room` 调用 `GET /api/rooms/{id}/slots`，展示聚合时间轴；不公开其他学生姓名和申请理由 |
| 第一版空间留言板 | 已接入 | 登录用户可查看；仅实际使用过该房间的用户可发布文字/单图留言，响应不暴露学号、班级和手机号 |

## 预约与使用流程

| 需求 | 状态 | 正式 v2 实现与验收点 |
|---|---|---|
| 四个使用场景 | 已接入 | `pages/reserve/reserve` 支持自习、开会、大型活动、音乐练习 |
| 后端自动分房 | 已接入 | 前端不传房间 ID；`POST /api/reservations` 返回最终分配房间 |
| 场景可用性 | 已接入 | 预约页按当前数据库角色、场景、日期、人数调用鉴权的 `GET /api/reservations/availability` |
| 连续时段选择 | 已接入 | 保留第一版时段格交互；除连续性和时长外，还要求整段候选房间交集非空 |
| 半小时粒度、单日最多4小时 | 已接入 | 默认 30 分钟、240 分钟；页面读取 `/api/reservations/config`，后端按有效状态累计并最终校验 |
| 可提前预约范围 | 已接入 | 页面允许今天至 `advance_days`；默认 7 天，后端再次校验 |
| 自习规则 | 已接入 | A102 → A101 → A105，共享；固定 1 人、用途“个人自习” |
| 开会规则 | 已接入 | A105 → A101 → A102，独占；用途至少 10 字 |
| A106 辅导员谈心 | 已接入 | 学生不可见候选且不可被分配；辅导员开会场景优先分配 A106，后端按数据库当前角色复核 |
| 大型活动 | 已接入 | 仅 A103，独占；用途至少 10 字 |
| 音乐练习 | 已接入 | B102 优先、A103 钢琴备选；A103 受配置开放时段限制并与大型活动互斥 |
| 玉兰卡核验 | 已接入 | `pages/reserve/form` 必须通过 `/api/reservations/campus-card-photo` 上传后才能提交；管理员审核页可预览 |
| 待审核 | 已接入 | 新申请状态为 pending，成功弹窗显示自动分配房间并跳转“我的” |
| 人工/批量审核 | 已接入 | `pages/admin/admin` 的预约审核标签调用 `/api/admin/reservations/review` |
| 23:00 自动审批 | 已接入（需部署验证） | 后端分钟任务按 `auto_approval_time` 处理；生产必须保证只有一个调度实例 |
| 取消 | 已接入 | `pages/my/my` 仅在截止时间前显示操作，后端再次执行截止校验 |
| 签到 | 已接入 | 我的页面支持时窗内按钮签到和扫描房间码签到；后端校验预约状态与时窗 |
| 房间签到二维码 | 部分接入 | 管理页可复制 `{ "room_id": ... }` 内容；二维码图片仍需外部生成或后续实现 |
| 清扫闭环 | 已接入 | 使用结束后在 `pages/my/my` 上传照片；管理员在清扫复核标签通过或退回 |
| 清扫阻断 | 已接入 | 未上传/被退回会阻断新预约；成功提交照片后先解锁，再异步复核 |
| 再次预约 | 已接入 | 我的预约对已终结状态提供再次预约入口，重新进入场景预约，不固定旧房间 |

## 违约、限制和管理员能力

| 需求 | 状态 | 正式 v2 实现与验收点 |
|---|---|---|
| 不设信用分 | 已接入 | v2 不恢复第一版信用分；学生查看预约限制状态，管理员查看违约/限制记录 |
| 未签到违约 | 已接入 | 超过签到宽限后生成幂等 no_show 记录并推进 missed 状态 |
| 清扫不合格违约 | 已接入 | 管理员退回清扫时记录 cleanup_failed，允许重传 |
| 累计3次自动禁约30天 | 已接入 | 阈值和天数来自系统参数；每达到一组阈值生成新的限时限制，不删除审计记录 |
| 人工限制 | 已接入 | 原生管理页支持临时、限时、自定义天数、永久限制和解除 |
| 用户搜索与记录 | 已接入 | 管理页可按姓名、学号、手机号、班级搜索，并查看违约和限制 |
| 系统参数 | 已接入 | 管理页可修改开放时间、时长、审批、提醒、违约和 A103 钢琴时段等参数 |
| 分房规则 | 已接入 | 管理页可修改优先级、容量、共享/独占和启用状态 |
| Excel 导出 | 已接入但原生无筛选 UI | 原生管理页下载完整 XLSX；后端支持筛选参数，但当前小程序未提供日期/场景/状态筛选控件 |
| 辅导员 CSV | 已接入 | 原生管理页可从微信文件选择 UTF-8 CSV 并上传；新账号凭据复制到管理员剪贴板并提示进入受控文档保存 |

## 微信消息与外部资源

| 需求 | 状态 | 正式 v2 实现与验收点 |
|---|---|---|
| 微信账号绑定 | 待平台配置 | 小程序调用 `wx.login`，后端用正式 AppID/AppSecret 换取 OpenID |
| 订阅授权 | 待平台配置 | 预约提交和“我的”均可请求授权；用户拒绝或调用失败不阻断预约 |
| 可靠消息队列 | 已接入（本地业务） | 提交、审核、提醒、清扫/限制相关事件进入 outbox，分钟任务异步发送 |
| 真机消息发送 | 待平台配置 | 需要正式 AppID、AppSecret、四类模板 ID、匹配的字段和学校 HTTPS 域名 |
| 照片持久化 | 已接入（需部署验证） | 留言图写入公开目录；玉兰卡/清扫照片进入独立私有目录，仅管理员鉴权读取并留审计，默认 90 天到期删除；生产需挂载持久化磁盘或对象存储 |
| 真机 API | 待学校部署 | `127.0.0.1` 仅模拟器可用；真机必须配置学校 HTTPS API 与微信合法域名 |

## 需求解释

1. 老师材料前文称 B102 为音乐专属房间，后文又允许使用 A103 钢琴。v2 统一解释为“B102 优先、A103 钢琴作为第二候选”，且不撤销已经成立的预约。
2. A103 的大型活动和音乐练习共用同一物理房间，因此按重叠时段互斥；B102 不应被错误联锁。
3. 姓名、学号、手机号来自登录账号，预约时不重复填写；管理员审核和授权导出仍可查看。
4. 房间详情只展示占用时段，不恢复第一版公开显示预约人姓名、班级和理由的做法，避免不必要的信息暴露。
5. “保留 V1 体验”指紫色视觉、空间导览、双页预约、连续时段、我的预约等产品骨架；第一版云函数、信用分、公开预约人详情和短信占位实现不进入正式 v2。

## 明确待办

### 需要开发继续完成

1. 确认并实现密码找回方案；当前没有短信验证码服务。
2. 如需内置签到二维码，在原生管理页生成/预览二维码；当前只能复制码内容。
3. 为历史数据量增长补充分页。
4. 如学校要求 H5，需单独确定正式 H5 主线；当前 `frontend/` 仅参考。

### 需要用户/学校完成

1. 提供并配置正式小程序 AppID，把开发者和体验人员加入项目。
2. 由学校运维保管 AppSecret，并写入服务器 `.env`。
3. 在微信公众平台申请四类订阅模板，确认字段编号并提供模板 ID。
4. 提供学校 HTTPS API 域名、证书、合法域名配置、服务器和 PostgreSQL。
5. 确定玉兰卡/清扫照片的隐私告知、访问权限、保存期限与删除策略。
6. 替换默认密钥和管理员账号，完成学生/管理员真机验收、体验版和上线审核。

在这些外部事项完成前，可以在微信开发者工具模拟器验收 REST 业务流程，但不能宣称真机网络、微信订阅消息或生产数据安全已经验收完成。
